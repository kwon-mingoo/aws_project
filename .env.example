# AWS2-GIOT-APP Environment Variables Example
# 이 파일을 복사해서 .env 파일로 만들고 실제 값으로 수정하세요

# 기본 애플리케이션 설정
NODE_ENV=production
PORT=3001
AWS_REGION=ap-northeast-2

# 도메인 설정 (Route 53에서 설정한 도메인)
DOMAIN_NAME=your-domain.com
ADDITIONAL_DOMAINS=api.your-domain.com,admin.your-domain.com

# AWS 자격 증명 (Parameter Store에서 관리 권장)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_SESSION_TOKEN=your-session-token

# S3 설정
S3_BUCKET_NAME=your-s3-bucket
S3_REGION=ap-northeast-2

# QuickSight 설정
QUICKSIGHT_ACCOUNT_ID=your-account-id
QUICKSIGHT_REGION=ap-northeast-2

# 챗봇/AI 설정
OPENAI_API_KEY=your-openai-api-key
CHATBOT_MODEL=gpt-3.5-turbo

# 데이터베이스 설정 (필요한 경우)
DATABASE_URL=your-database-url
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=aws2_giot_db

# 로그 설정
LOG_LEVEL=info
LOG_FILE_PATH=/var/log/aws2-giot-app

# 보안 설정
JWT_SECRET=your-jwt-secret
CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# 모니터링 설정
HEALTH_CHECK_TIMEOUT=5000
METRICS_ENABLED=true

# 개발 환경에서만 사용
FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:3001